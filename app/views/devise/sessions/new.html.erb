<div class="sign_in">
   <h2>Digita los datos</h2>

   <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
     <p><%= f.label :login, "Usuario" %><br />
     <%= f.text_field :login %></p>

     <p><%= f.label :password, "Contraseña" %><br />
     <%= f.password_field :password %></p>

     <p><%= f.submit "ENTRAR" %></p>
   <% end %>
</div>